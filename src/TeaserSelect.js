import React, {Component, PropTypes} from 'react';
import {DragSource} from 'react-dnd';

export default class TeaserSelect extends Component {

    render()
    {
        return (
            <div className="select">TEASERSELECT</div>
        );
    }
}